{
  "version": 2,
  "builds": [
    {
      "src": "*.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/medicine", "dest": "/server/server.mjs" }
  ]
}
